<Window x:Class="TreeView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeView"
        xmlns:MyNamespace="clr-namespace:TreeViewExplorerControl;assembly=TreeViewExplorerControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <StackPanel>
            <MyNamespace:ExplorerControl x:Name="ExplorerControl" PathToWatch="{Binding Paths}"  Background="#FFC31919"></MyNamespace:ExplorerControl>
            <TextBlock Text="{Binding ElementName=ExplorerControl, Path=PathToWatch, Mode=TwoWay}"></TextBlock>
            <TextBlock Text="{Binding ElementName=ExplorerControl, Path=SelectedItemName}"></TextBlock>
            <TextBlock Text="{Binding ElementName=ExplorerControl, Path=SelectedItemPath}"></TextBlock>
            <TextBlock Text="{Binding ElementName=ExplorerControl, Path=SelectedItemIsFolder}"></TextBlock>
            <TextBlock Text="{Binding ElementName=ExplorerControl, Path=Nodes.Count}"></TextBlock>
            <Button Click="ButtonBase_OnClick">Cambiar</Button>
            <TextBlock Text="{Binding Paths}"></TextBlock>

        </StackPanel>
      
    </Grid>
</Window>
