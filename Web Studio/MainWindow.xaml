<controls:MetroWindow x:Class="Web_Studio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:localization="clr-namespace:Web_Studio.Localization"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Web Studio" TitleCaps="False" Height="350" Width="525">
    <Grid>
        <Menu IsMainMenu="True">
            <MenuItem Header="{x:Static localization:Strings.File}">
                <MenuItem Header="{x:Static localization:Strings.New_File}"></MenuItem>
                <MenuItem Header="{x:Static localization:Strings.New_Project}"></MenuItem>
            </MenuItem>
            <MenuItem Header="{x:Static localization:Strings.Edit}">
                <MenuItem Header="{x:Static localization:Strings.Options}"></MenuItem>
            </MenuItem>
            <MenuItem Header="{x:Static localization:Strings.View}"></MenuItem>
            <MenuItem Header="{x:Static localization:Strings.Window}"></MenuItem>
            <MenuItem Header="{x:Static localization:Strings.Help}"></MenuItem>
        </Menu>
        <avalonEdit:TextEditor
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
    Name="TextEditor"
    FontFamily="Consolas"
    SyntaxHighlighting="HTML"
    FontSize="10pt" Margin="10,40,10,0" Foreground="White"/>
    </Grid>
</controls:MetroWindow>
