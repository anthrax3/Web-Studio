<controls:MetroWindow x:Class="Web_Studio.NewProject"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:lex="http://wpflocalizeextension.codeplex.com"
                      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
                      lex:LocalizeDictionary.DesignCulture="es"
                      lex:ResxLocalizationProvider.DefaultAssembly="Web Studio"
                      lex:ResxLocalizationProvider.DefaultDictionary="Strings"
                      mc:Ignorable="d" Height="500" Width="500" ResizeMode="NoResize"
                      Title="{lex:Loc WizardCreateNewProject}">

    <xctk:Wizard HelpButtonVisibility="Hidden" CanHelp="False" CancelButtonContent="{lex:Loc Cancel}"
                 BackButtonContent="{lex:Loc Back}"
                 NextButtonContent="{lex:Loc Next}" FinishButtonContent="{lex:Loc Finish}"
                 Background="{DynamicResource WindowBackgroundBrush}"
                 ExteriorPanelMinWidth="0" BorderBrush="{x:Null}" BorderThickness="0" Name="Wizard"
                 Finish="Wizard_Finish">
        <xctk:WizardPage x:Name="IntroPage" Description="{lex:Loc WizardDescription}"
                         CanSelectNextPage="{Binding ElementName=Window, Path=NextPageEnabled}"
                         Background="{DynamicResource WindowBackgroundBrush}"
                         HeaderBackground="{DynamicResource AccentColorBrush}">
            <Grid VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Label Content="{lex:Loc WizardProjectName}" Grid.Row="0" Grid.Column="0" />
                <TextBox Name="tbProjectName" Grid.Row="0" Grid.Column="1" TextChanged="TextBox_TextChanged"
                         Text="{Binding Name}" />
                <Label Content="{lex:Loc WizardPath}" Grid.Row="1" Grid.Column="0" />
                <TextBox Name="tbPath" Grid.Row="1" Grid.Column="1" TextChanged="TextBox_TextChanged"
                         Text="{Binding FullPath}" />
                <Button Name="bBrowser" Content="{lex:Loc Browse}" Grid.Row="1" Grid.Column="2"
                        Click="ButtonBrowser_OnClick" Margin="10,0,0,0" HorizontalAlignment="Left"
                        VerticalAlignment="Center" />
            </Grid>
        </xctk:WizardPage>
        <xctk:WizardPage x:Name="Page1" PageType="Interior" Background="{DynamicResource WindowBackgroundBrush}"
                         CanFinish="True" CanSelectNextPage="False">
            <Button>AAAA</Button>
        </xctk:WizardPage>
    </xctk:Wizard>

</controls:MetroWindow>