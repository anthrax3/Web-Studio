namespace Web_Studio.Events
{
    /// <summary>
    ///     Inform the project changed
    /// </summary>
    public class ChangedProjectEvent
    {
        //We don't need data because ProjectModel has a static instance, Singleton pattern.
    }
}