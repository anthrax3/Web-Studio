<UserControl x:Class="Web_Studio.Editor.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:editor="clr-namespace:Web_Studio.Editor"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <editor:TextEditorMvvm Document="{Binding Document}"   FontFamily="Consolas" Margin="0" Foreground="White" ShowLineNumbers="{Binding EditorShowLineNumbers}" FontSize="{Binding EditorFontSize}"
                                    HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
